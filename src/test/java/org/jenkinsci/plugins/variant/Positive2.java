package org.jenkinsci.plugins.variant;

import jenkins.model.Jenkins;

/**
 * @author Kohsuke Kawaguchi
 */
@OptionalExtension(requireClasses=Jenkins.class, requirePlugins="credentials")
public class Positive2 extends Base {
}
