/**
 * @author Kohsuke Kawaguchi
 */
@OptionalPackage(requireClasses=Jenkins.class)
package org.jenkinsci.plugins.variant.pkg;

import jenkins.model.Jenkins;
import org.jenkinsci.plugins.variant.OptionalPackage;